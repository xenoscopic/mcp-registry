[
    {
        "name": "get-schema",
        "description": "Introspect labels, relationship types, and property keys to provide schema context for Neo4j.",
        "arguments": []
    },
    {
        "name": "read-cypher",
        "description": "Execute arbitrary read-only Cypher queries against the configured Neo4j database. Rejects writes, schema/admin operations, and PROFILE queries.",
        "arguments": [
            {
                "name": "query",
                "type": "string",
                "desc": "Cypher query to execute in read mode. Must not perform writes, admin, schema, or PROFILE operations."
            }
        ]
    },
    {
        "name": "write-cypher",
        "description": "Execute arbitrary write Cypher queries (including schema/admin operations) against the configured Neo4j database. Use only in non-production environments.",
        "arguments": [
            {
                "name": "query",
                "type": "string",
                "desc": "Cypher query to execute in write mode. Can perform writes, schema changes, and admin operations."
            }
        ]
    },
    {
        "name": "list-gds-procedures",
        "description": "List available Neo4j Graph Data Science (GDS) procedures in the connected Neo4j instance.",
        "arguments": []
    }
]
