name: pica
image: mcp/pica-mcp-server
type: server
meta:
  category: integration
  tags:
    - integration
    - api
    - automation
    - productivity
about:
  title: Pica
  description: Provides access to Pica's integrations, actions, execution, and code generation capabilities.
  icon: https://assets.picaos.com/github/mcp.svg
source:
  project: https://github.com/picahq/mcp
  branch: main
  commit: cd136f1b8fc1baee1f8952494832643b5b82e948
config:
  description: Configure the connection to Pica MCP server
  secrets:
    - name: pica.api_key
      env: PICA_SECRET
      example: sk_live_example_key_replace_with_your_actual_pica_secret
  env:
    - name: PICA_BASE_URL
      example: https://api.picaos.com
      value: "{{pica.base_url}}"
  parameters:
    type: object
    properties:
      base_url:
        type: string
        default: "https://api.picaos.com"
        description: "The Pica API base URL"
