name: wayfound
type: remote
meta:
  category: ai
  tags:
    - ai
about:
  title: Wayfound MCP
  description: Wayfound’s MCP server allows business users to govern, supervise, and improve AI Agents
  icon: https://cdn.prod.website-files.com/682a0b46722bcd80642ab415/68377ba02e561e382ca5af4c_wayfound-logo-small.svg
remote:
  transport_type: sse
  url: https://app.wayfound.ai/sse
  headers:
    Authorization: "Bearer {{wayfound.mcp_api_key}}"
config:
  secrets:
    - name: wayfound.mcp_api_key
      env: MCP_API_KEY
      example: <MCP_API_KEY>
